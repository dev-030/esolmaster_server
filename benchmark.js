// This script exists for the user to run manual benchmarks if they choose to
// node benchmark.js
console.log("Run the backend and click 'Import' to see the newly added Benchmark logs in your terminal.");
