model Criterion {
  id          String     @id @default(nanoid())
  code        String     @unique
  description String
  questions   Question[]
}

enum AwardingBody {
  ESB
  ASCENTIS
  GATEWAY
  TRINITY
}

enum PassLogicType {
  CRITERIA_ONLY
  SCORE_ONLY
  CRITERIA_AND_SCORE
}
