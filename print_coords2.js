const fs = require('fs');
const appFile = 'src/task/task.service.ts'; // Just to check what was saved
const runFile = 'run_benchmark.ts'; 

// Let's just run test_single.ts and print the coordinates directly!
